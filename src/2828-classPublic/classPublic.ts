type ClassPublicKeys<T> = T extends infer F ? keyof F : T 


/* _____________ Test Cases _____________ */
import type { Equal, Expect } from '../../utils'

class A {
  public str: string
  protected num: number
  private bool: boolean
  constructor() {
    this.str = 'naive'
    this.num = 19260917
    this.bool = true
  }

  getNum() {
    return Math.random()
  }
}

type cases = [
  Expect<Equal<ClassPublicKeys<A>, 'str' | 'getNum'>>,
]
